{
  "name": "cat-each",
  "version": "1.0.0",
  "description": "Creates a stream by concatenating new streams, each created by a factory from a list",
  "main": "cat-each.js",
  "scripts": {
    "test": "./node_modules/.bin/tape test/*.js | ./node_modules/.bin/tap-spec"
  },
  "devDependencies": {
    "tap-spec": "^4.1.1",
    "tape": "^4.4.0",
    "tape-catch": "^1.0.4"
  },
  "author": "Chris Taylor <chris@22tabs.com> (https://blog.22tabs.com)",
  "license": "MIT",
  "dependencies": {
    "async": "^1.5.2"
  }
}
